// Gradient text with better visibility
export const gradientText = 'bg-gradient-to-r from-primary via-primary/80 to-primary/60 bg-clip-text text-transparent font-bold';

// Page background
export const pageBackground = 'bg-background dark:bg-background';

// No grid pattern, just a clean background
export const gridPattern = '';

// Section container base styles
export const sectionContainer = 'w-full px-4 sm:px-6 lg:px-8 relative z-10';

// Card style with glass-morphism effect and improved transitions
export const cardStyle = 'w-full bg-background/80 dark:bg-gray-900/80 backdrop-blur-sm rounded-2xl shadow-lg p-6 md:p-8 transition-all duration-300 border border-border/30 dark:border-gray-800/50 hover:border-primary/40 hover:shadow-xl hover:shadow-primary/10 focus-within:ring-2 focus-within:ring-primary/50 focus-within:ring-offset-2 dark:focus-within:ring-offset-gray-900 hover:-translate-y-0.5';

export interface SocialLinks {
  whatsapp: string;
  github: string;
  linkedin: string;
}

export interface ContactInfo {
  name: string;
  email: string;
  phone: string;
  workingHours: string;
  socialLinks: SocialLinks;
}

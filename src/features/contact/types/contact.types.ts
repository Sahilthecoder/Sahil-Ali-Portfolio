export interface SocialLinks {
  whatsapp: string;
  github: string;
  linkedin: string;
}

export interface ContactInfo {
  email: string;
  phone: string;
  workingHours: string;
  socialLinks: SocialLinks;
}
